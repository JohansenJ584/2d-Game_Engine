//#include "Engine.hpp" // The main engine
//#include "GraphicsEngineRenderer.hpp" // The rendering engine

int main(int argc, char** argv) {
	/*
	// Create an instance of an object for our engine
	Engine engine;

	// Initialize the engine subsystems
	engine.InitializeGraphicsSubSystem();

    // Once all subsystems have been initialized, start the engine
    engine.Start();

    // Run our program forever
	engine.MainGameLoop();

    // Explicitly call shutdown to terminate our engine
    engine.Shutdown();
	*/
	// When our program ends, it will exit scope, call the destructor, and clean up the program.
	return 0;
}
